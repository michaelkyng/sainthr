<script setup lang="ts">
import { cn } from '~/lib/utils';

const props = defineProps<{ class?: string; disabled?: boolean }>();
const model = defineModel<boolean>({ default: false });
</script>

<template>
  <button
    type="button"
    role="switch"
    :aria-checked="model"
    :disabled="props.disabled"
    :class="
      cn(
        'relative inline-flex h-5 w-9 shrink-0 cursor-pointer rounded-full border-2 border-transparent transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-green disabled:cursor-not-allowed disabled:opacity-50',
        model ? 'bg-green' : 'bg-line',
        props.class,
      )
    "
    @click="model = !model"
  >
    <span
      :class="[
        'pointer-events-none inline-block size-4 rounded-full bg-white shadow transition-transform',
        model ? 'translate-x-4' : 'translate-x-0',
      ]"
    />
  </button>
</template>
